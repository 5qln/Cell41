#!/usr/bin/env python3
"""fixtures — the integration round's declared fixture apparatus:
the pinned R06 desk-harness binding, the fixture-world builder, the
plan-equivalence reference runner (C3), the cold-restart/run-lock
runner (lens 5), the pinned scenarios, the enforcement fixtures with
the deliberately-injected violations (C4), and the soft-config
fixtures.  Deterministic fixture fiction, clearly labelled — nothing
here touches the live box and nothing writes the live ledger
(H-INT-1)."""
