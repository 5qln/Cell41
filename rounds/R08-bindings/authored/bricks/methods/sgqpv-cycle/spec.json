{
  "spec_version": 1,
  "round": "bindings-brick",
  "work_dir": "/home/deploy/the-cell/state",
  "scenario": "word.json",
  "ledger": "/home/deploy/the-cell/state/gates.jsonl",
  "trail": "/home/deploy/the-cell/state/trail.jsonl",
  "live_socket": null,
  "socket_dir": null,
  "materialize": null,
  "materialized": null,
  "soft_config": "/home/deploy/.config/herdr/soft.json",
  "observe_states": true,
  "block_version": "",
  "scope": "bindings-brick-sgqpv-cycle",
  "clock": null,
  "wait_timeout_ms": 120000,
  "timeout_s": 30.0,
  "max_steps": null,
  "notes": {
    "brick": "methods/sgqpv-cycle — the single 4+1 cycle, a CANDIDATE method (D2/D4 — the acceptance word is Amihai's to choose; this brick is a demonstration of the C8 format, never a claimed acceptance word)",
    "scenario": "relative to this spec file's directory (declared resolution) — word.json is decoded by the engine's own reader",
    "ledger": "the live gates ledger — a real /conduct over this brick appends per-gate records there (never run by the authoring pass, H-R08-1)",
    "trail": "the live formation trail — the file the podium now renders",
    "live_socket": "null = the engine's own resolution (HERDR_SOCKET_PATH → ~/.config/herdr/herdr.sock) — never resolved in the wrapper",
    "soft_config": "H-BRIDGE-2's provisional location — absent today, so the engine's declared defaults apply honestly (W3 will write it; H-R08-5)",
    "wait_timeout_ms": "PROVISIONAL (H-INT-4): 120000 — declared data, corrected by the first real run (W4)",
    "timeout_s": "PROVISIONAL: 30.0 — declared data, corrected by the first real run (W4)",
    "max_steps": "null = no caller step budget — the word's length is the walk's bound"
  }
}
