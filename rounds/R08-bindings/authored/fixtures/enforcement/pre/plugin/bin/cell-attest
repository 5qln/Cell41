#!/usr/bin/env python3
"""fixture cell-attest — the PRE-integration state, finding iii: the
ledger is imported directly (unpinned).  Declared fixture apparatus:
this file exists ONLY to prove the enforcement suite fails the pre
state, and it is replaced by the authored re-pointed attest
(authored/enforcement/plugin-bin/cell-attest — the ledger through the
seam contract) before the suite may read clean.  Never the production
surface."""
from __future__ import annotations

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
sys.path.insert(0, "/home/deploy/the-cell/ledger")
import fractal_ledger  # noqa: E402  (the finding: direct, unpinned)


def main():
    print("fixture stand-in — the PRE-integration attest imports the "
          "ledger directly (the finding); never run, never the "
          "production surface")
    return 2


if __name__ == "__main__":
    sys.exit(main())
