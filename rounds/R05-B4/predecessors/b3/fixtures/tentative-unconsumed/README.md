# fixture — tentative-unconsumed (a tentative node consumed)

Start state as lawful-3deep; step 2 declares a downstream gate that consumed the tentative seed as evidence.

Prediction: refused — a tentative node is non-data; no heuristic promotes it and no downstream gate consumes it (C5, T-R5-02). The descent never writes the podium.
