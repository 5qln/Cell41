"""fixtures — the bridge's fixture apparatus (declared test fiction,
never the conductor): the live-server stand-in for the live herdr box,
the live-mode run, the soft-config override/malformed files, the
cold-restart harness, and the builder that renders the specs and pins
the expected bytes.

Nothing here is a claim about the live box: the live server models the
probed pane state (commission §3) and the deterministic answers are
clearly labelled fixture fiction (B4's fixture-desk precedent).
"""
