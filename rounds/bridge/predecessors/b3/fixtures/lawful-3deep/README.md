# fixture — lawful-3deep (a 3-deep descent)

Start state: the root cell `_` with a human-planted question (fixture fiction) attested at gate x, gate y attested, gate z PROPOSED and failing to lock — the descent trigger.

Prediction: the walk descends ε → Q → PQ → GPQ with byte-identical `axis.field` from root to leaf; the leaf's V is attested and carries artifact + ∞0′ — the return criterion. `expected/` pins the byte-exact node tree and ledger.
